Module xerial.RawMigration
==========================

Functions
---------

    
`nullTransfer(raw: List[Dict[~KT, ~VT]]) ‑> List[Dict[~KT, ~VT]]`
:   

Classes
-------

`RawMigration(config)`
:   

    ### Methods

    `close(self)`
    :

    `connect(self)`
    :

    `dump(self, dataPath: str, transfer: Callable[[List[Dict[~KT, ~VT]]], List[Dict[~KT, ~VT]]] = <function nullTransfer>)`
    :